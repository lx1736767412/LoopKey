[project]
name = "loopkey"
version = "0.1.0"
description = "A desktop automation macro tool for key sequence recording and playback"
requires-python = ">=3.12"
dependencies = [
    "pynput>=1.8.0",
    "keyboard>=0.13.5",
]
